---
import type { Lang, Translations } from '../i18n/ui';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import FloatingContact from '../components/FloatingContact.astro';
import ContactModal from '../components/ContactModal.astro';
import '../styles/global.css';

interface Props {
  lang: Lang;
  t: Translations;
  title?: string;
  description?: string;
}

const { lang, t, title, description } = Astro.props;
const pageTitle = title ?? t.meta.title;
const pageDescription = description ?? t.meta.description;
const canonical = new URL(Astro.url.pathname, Astro.site);
---

<!doctype html>
<html lang={lang}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={pageDescription} />
    <meta name="theme-color" content="#05070b" media="(prefers-color-scheme: dark)" />
    <meta name="theme-color" content="#f3f5f7" media="(prefers-color-scheme: light)" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="canonical" href={canonical} />
    <meta name="generator" content={Astro.generator} />
    <title>{pageTitle}</title>

    <meta property="og:type" content="website" />
    <meta property="og:title" content={pageTitle} />
    <meta property="og:description" content={pageDescription} />
    <meta property="og:url" content={canonical} />
    <meta property="og:image" content="/logos/logo-light-bg.png" />
    <meta name="twitter:card" content="summary_large_image" />

    <script is:inline>
      (function () {
        try {
          if (sessionStorage.getItem('tc-ready') === '1') {
            document.documentElement.classList.add('app-ready');
          }
        } catch (e) {}
        var saved = localStorage.getItem('theme');
        document.documentElement.classList.toggle('dark', saved ? saved === 'dark' : false);
      })();
    </script>
  </head>
  <body class="min-h-screen bg-bg text-ink">
    <!-- Premium Layout Preloader Overlay -->
    <div
      id="page-preloader"
      class="fixed inset-0 z-[9999] flex flex-col items-center justify-center bg-[#05070b] transition-all duration-[800ms] cubic-bezier(0.16, 1, 0.3, 1)"
    >
      <div class="relative flex flex-col items-center">
        <!-- Circular spinner with premium glows -->
        <div class="relative h-14 w-14">
          <div class="absolute inset-0 rounded-full border-t-2 border-r-2 border-accent animate-spin"></div>
          <div class="absolute inset-2 rounded-full border-b-2 border-l-2 border-accent-bright animate-spin" style="animation-direction: reverse; animation-duration: 0.8s"></div>
        </div>
        <!-- Soft, clean loading text -->
        <div class="mt-6 font-display text-[0.85rem] font-bold uppercase tracking-[0.2em] text-[#64748b]">
          Turon<span class="text-white">Core</span>
        </div>
      </div>
    </div>

    <!-- Preloader controller script -->
    <script is:inline>
      (function () {
        if (document.documentElement.classList.contains('app-ready')) {
          const preloader = document.getElementById('page-preloader');
          if (preloader) preloader.remove();
          return;
        }

        document.documentElement.classList.add('lenis-stopped');

        window.addEventListener('load', function () {
          const preloader = document.getElementById('page-preloader');
          try {
            sessionStorage.setItem('tc-ready', '1');
            document.documentElement.classList.add('app-ready');
          } catch (e) {}
          if (preloader) {
            preloader.style.opacity = '0';
            preloader.style.visibility = 'hidden';
            preloader.style.transform = 'translateY(-24px)';

            setTimeout(function () {
              preloader.remove();
              document.documentElement.classList.remove('lenis-stopped');
              
              const heroReveals = document.querySelectorAll('#hero .reveal, #hero .reveal-scale');
              heroReveals.forEach(function (el) {
                el.classList.add('visible');
              });
            }, 850);
          }
        });
      })();
    </script>

    <div id="scroll-progress"></div>

    <a
      href="#main"
      class="btn btn-primary sr-only px-5 py-2.5 text-sm focus:not-sr-only focus:absolute focus:left-4 focus:top-4 focus:z-[100]"
    >
      Skip to content
    </a>

    <Header lang={lang} t={t} />

    <main id="main">
      <slot />
    </main>

    <Footer lang={lang} t={t} />

    <FloatingContact lang={lang} t={t} />

    <ContactModal lang={lang} t={t} />

    <script>
      import { bindPage } from '../scripts/page.ts';
      import { initLangSwitchers } from '../scripts/lang.ts';
      import { initSmoothScroll } from '../scripts/scroll.ts';

      bindPage();
      initLangSwitchers();
      initSmoothScroll();
    </script>
  </body>
</html>
